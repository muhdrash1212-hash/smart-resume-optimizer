services:
  - type: web
    name: smart-resume-optimizer
    env: docker
    dockerfilePath: Dockerfile
    plan: free
    regions:
      - oregon
    envVars:
      - key: CERABRAS_API_KEY
        sync: false
      - key: PYTHONUNBUFFERED
        value: "1"